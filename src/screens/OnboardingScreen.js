import React from 'react';
import { View, Text } from 'react-native';

// Pantalla de Onboarding: muestra consentimiento y permite seleccionar el perfil del usuario
const OnboardingScreen = () => (
  <View>
    {/* Aquí se mostrará el consentimiento y la selección de perfil */}
    <Text>Pantalla Onboarding: consentimiento y selección de perfil</Text>
  </View>
);

export default OnboardingScreen;
