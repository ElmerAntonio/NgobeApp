# Infraestructura y despliegue

Aquí puedes colocar `Dockerfile`, `docker-compose.yml` y configuraciones de despliegue.

Sugerencia inicial: crear `docker-compose.yml` con servicios:

- postgres
- minio (para S3 local)
- backend

No hay configuración incluida por defecto para evitar duplicados; crearé plantillas bajo petición.
