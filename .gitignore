# Expo y React Native
node_modules/
.expo/
dist/
npm-debug.*
yarn-debug.*
yarn-error.*
web-build/

# macOS (Archivos del sistema)
.DS_Store

# Archivos de certificados y llaves (¡No borrar!)
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Variables de entorno secretas (Seguridad)
.env
.env.*
.env.local
!.env.example
!backend/.env.example
node_modules
.pnpm-store

# Estado local del CLI de Supabase (no debe subirse al repo)
supabase/.temp/

# IDEs (Android Studio, WebStorm, VS Code, etc.)
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
